const sum = (n1, n2) => {
  console.log(n1 + n2);
};
sum(11, 2);

// using return inside the function

const add = (n1, n2) => {
  return n1 + n2;
};

const result = add(10 , 10);
console.log(result);