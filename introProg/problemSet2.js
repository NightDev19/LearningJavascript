/*
    1. Declare a variable using let with any name you like, but without a value.
    2. On a separate line, assign the variable String : "moo!".
    3. Log out this variable to the terminal using console.log();

    Repeat the above steps but try using a const variable, and see what happens
*/

let cow;
cow = "Moo!";
console.log("What the cow said?\nCow : " + cow);

/*
    const Coo; <- const cant assign at all || Solution const Cow = "Moo!";
    Cow = "Moo!"; <- like this so it gives error
    console.log("What the cow said?\nCow : " + Cow);

    PS: This set of code must have an error
*/