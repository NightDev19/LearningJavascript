/*
    Create a program that starts with a variable number.
    The program will print out whether the variable is 
    even or add to the screen when run

    Change the variables to test it works in both cases.

    "HINT" : Modulus operator is your friend
*/

let num = 99;

if(num % 2 === 0){
    console.log("even");
}else{
    console.log("odd");
}